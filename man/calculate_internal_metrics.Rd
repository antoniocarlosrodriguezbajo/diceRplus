% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment_funcions.R
\name{calculate_internal_metrics}
\alias{calculate_internal_metrics}
\title{Compute Internal Clustering Metrics via diceR Consensus Evaluation}
\usage{
calculate_internal_metrics(data, cluster_labels, nk)
}
\arguments{
\item{data}{A numeric data frame or matrix of features (samples in rows, variables in columns).}

\item{cluster_labels}{A vector of cluster assignments for each observation.}

\item{nk}{An integer specifying the number of clusters used. Used as a dimension label.}
}
\value{
A named list containing internal clustering metric values as computed by diceR:
typically including \code{connectivity}, \code{dunn}, and \code{silhouette}.
}
\description{
Computes a set of internal validation metrics (e.g., connectivity, Dunn index, silhouette)
for a single clustering solution using \code{consensus_evaluate()} from the diceR package.
}
\details{
This function wraps a single clustering solution into the expected four-dimensional array
format required by \code{consensus_evaluate()}, assigning dummy labels for repetition and algorithm.
}
\examples{
\dontrun{
library(diceR)
data(iris)
cl_labels <- kmeans(iris[, -5], centers = 3)$cluster
calculate_internal_metrics(iris[, -5], cl_labels, nk = 3)
}

}
