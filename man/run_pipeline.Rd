% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/experiment_funcions.R
\name{run_pipeline}
\alias{run_pipeline}
\title{Execute Full Clustering Pipeline with and without Feature Selection}
\usage{
run_pipeline(
  data,
  dataset_name,
  top_features,
  nk,
  algorithms,
  UFS_method = "Inf-FS2020",
  alpha = 0.1,
  n_reps = 1,
  seed = 100
)
}
\arguments{
\item{data}{A numeric matrix or data frame with observations in rows and features in columns.}

\item{dataset_name}{A character string used to label the dataset across experiments.}

\item{top_features}{A vector of selected feature indices or names to use for clustering with UFS.}

\item{nk}{Integer. Number of clusters to use in all clustering methods.}

\item{algorithms}{A character vector of clustering algorithms to evaluate (e.g., \code{"kmeans"}, \code{"spectral"}).}

\item{UFS_method}{A string indicating the unsupervised feature selection technique used (default: \code{"Inf-FS2020"}).}

\item{alpha}{Numeric hyperparameter passed to the UFS method (default: \code{0.1}).}

\item{n_reps}{Not currently used. Reserved for future extension with multiple seeds or replicates. Default is \code{1}.}

\item{seed}{Integer seed for reproducibility (default: \code{100}).}
}
\value{
A named list containing three experiment IDs:
\describe{
\item{\code{best_experiment_UFS_id}}{ID of the best clustering experiment using UFS.}
\item{\code{best_experiment_NO_UFS_id}}{ID of the best experiment without UFS.}
\item{\code{best_experiment_UFS_RPClu_id}}{ID of the RPClu experiment using UFS.}
}
}
\description{
Runs a comprehensive clustering pipeline that compares the performance of multiple ensemble clustering strategies—
with and without unsupervised feature selection (UFS)—and includes RPClu as a competing method. Results are logged
as experiments with associated metadata and internal validation metrics.
}
\details{
This pipeline performs three evaluations:
\enumerate{
\item Clustering with unsupervised feature selection and selection of the best ensemble.
\item Clustering without feature selection, using all available features.
\item Execution of RPClu ensemble clustering using selected features.
}
Internal clustering metrics are computed for all approaches using \code{calculate_internal_metrics()},
and the results are saved via \code{save_experiment()}.
}
\examples{
\dontrun{
run_pipeline(
  data = iris[, -5],
  dataset_name = "iris",
  top_features = c(1, 2, 3),
  nk = 3,
  algorithms = c("kmeans", "spectral")
)
}

}
